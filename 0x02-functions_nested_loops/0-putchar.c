#include <main.h>

/**
 * main - if the character is _putchar
 *
 * Description - using the main function
 * this program prints "alphabets"
 * Return: 0
 */

int main(void)
{
	char ch;

	for (ch = '_' ; ch = 'p' ; ch = 'u' ; ch = 't' ; ch = 'c' ; ch = 'h' ; ch = 'h' ; ch = 'a' ; ch = 'r')
	{
		printf("_purchar \n");
	}
	return (0);
}

