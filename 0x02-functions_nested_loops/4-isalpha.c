#include "main.h"

/**
 * _isalpha - function to check if c is a letter, lowercase or uppercase
 * @c: is the int that will use forthe argument of the function
 * Return: 0
 */

int -isalpha(int c)
{
	if ((c >= 'a' && c <= 'z' || (c >= 'A' && C <= 'Z'))
			{
			return (1);
			}
			else
			return (0);
}
