#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Indefinite loop incoming :(\n");

			1 = 0;
			/*
			 * while (1 < 10)
			 * {
			 * putchar(1);
			 * }
			 */

			print("Infinite loop avoided! \\o/\n");

			return (0);
}
